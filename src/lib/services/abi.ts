export const LIMIT_ORDER_ABI = [
    "function executeOrderLimitTrade(address base,address quote,uint256 amount,uint256 price,uint256 expiry,uint8 side,uint256 slippageBps) returns (bytes32 orderId)"
  ];
